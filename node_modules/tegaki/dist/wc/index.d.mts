import { A as LineCap, B as TegakiSingletonEffectName, C as resolveEffects, D as CSSLength, E as COMPATIBLE_BUNDLE_VERSIONS, F as TegakiEffectConfigs, I as TegakiEffectName, L as TegakiEffects, M as Point, N as Stroke, O as FontOutput, P as TegakiBundle, R as TegakiGlyphData, S as ResolvedEffect, T as BUNDLE_VERSION, V as TimedPoint, _ as computeTimeline, a as CreateElementFn, b as ensureFontFace, c as TimeControlMode, d as getBundle, f as registerBundle, g as TimelineEntry, h as TimelineConfig, i as TegakiEngine, j as PathCommand, k as GlyphData, l as TimeControlProp, m as Timeline, n as buildRootProps, o as TegakiEngineOptions, p as resolveBundle, r as domCreateElement, s as TegakiQuality, t as buildChildren, u as createBundle, v as TextLayout, w as BBox, x as drawGlyph, y as computeTextLayout, z as TegakiMultiEffectName } from "../index-DtvcCtXG.mjs";

//#region src/wc/TegakiElement.d.ts
declare class TegakiElement extends HTMLElement {
  static observedAttributes: ("text" | "font" | "time" | "speed" | "duration" | "playing" | "loop" | "delay" | "loop-gap" | "pixel-ratio" | "segment-size" | "show-overlay" | "direction")[];
  private _engine;
  private _container;
  private _font;
  private _effects;
  private _timing;
  private _quality;
  private _onComplete;
  constructor();
  connectedCallback(): void;
  disconnectedCallback(): void;
  attributeChangedCallback(_name: string, _oldValue: string | null, _newValue: string | null): void;
  /** The underlying engine instance. */
  get engine(): TegakiEngine | null;
  /** Set the font bundle directly (alternative to the `font` attribute for registered names). */
  get font(): TegakiBundle | string | undefined;
  set font(value: TegakiBundle | string | undefined);
  /** Visual effects configuration. */
  get effects(): TegakiEngineOptions['effects'];
  set effects(value: TegakiEngineOptions['effects']);
  /** Timeline timing configuration. */
  get timing(): TegakiEngineOptions['timing'];
  set timing(value: TegakiEngineOptions['timing']);
  /** Render-quality configuration (supersampling, segment subdivision). */
  get quality(): TegakiEngineOptions['quality'];
  set quality(value: TegakiEngineOptions['quality']);
  /** Callback when animation completes. */
  get onComplete(): (() => void) | undefined;
  set onComplete(value: (() => void) | undefined);
  play(): void;
  pause(): void;
  seek(time: number): void;
  restart(): void;
  get currentTime(): number;
  get duration(): number;
  get isPlaying(): boolean;
  get isComplete(): boolean;
  private _buildOptions;
  /**
   * Merge the `quality` JS property with `pixel-ratio` / `segment-size`
   * attribute shortcuts. Attributes override properties on a per-field basis.
   */
  private _resolveQuality;
  private _resolveTime;
  private _getNumberAttr;
}
/**
 * Register the `<tegaki-renderer>` custom element.
 * Call this once before using the element in HTML.
 *
 * @param tagName - Custom element tag name. Default: `'tegaki-renderer'`.
 *   Note: custom element names must contain a hyphen per the HTML spec.
 */
declare function registerTegakiElement(tagName?: string): void;
//#endregion
export { BBox, BUNDLE_VERSION, COMPATIBLE_BUNDLE_VERSIONS, CSSLength, CreateElementFn, FontOutput, GlyphData, LineCap, PathCommand, Point, ResolvedEffect, Stroke, TegakiBundle, TegakiEffectConfigs, TegakiEffectName, TegakiEffects, TegakiElement, TegakiEngine, TegakiEngineOptions, TegakiGlyphData, TegakiMultiEffectName, TegakiQuality, TegakiSingletonEffectName, TextLayout, TimeControlMode, TimeControlProp, TimedPoint, Timeline, TimelineConfig, TimelineEntry, buildChildren, buildRootProps, computeTextLayout, computeTimeline, createBundle, domCreateElement, drawGlyph, ensureFontFace, getBundle, registerBundle, registerTegakiElement, resolveBundle, resolveEffects };
//# sourceMappingURL=index.d.mts.map